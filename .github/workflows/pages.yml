name: Deploy demo to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - demo/**
      - src/**
      - .github/workflows/pages.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Bundle demo into site/
        # --external keeps the optional @napi-rs/canvas Node.js backend out of the
        # browser bundle; it's only ever reached via a runtime dynamic import() in
        # plain Node.js, and bun's bundler otherwise tries to inline its native
        # .node binding and fails with a browser target.
        run: bun build demo/main.ts --outdir site --target browser --minify --external @napi-rs/canvas

      - name: Copy static assets
        run: |
          cp demo/index.html site/index.html
          if [ -d demo/assets ]; then cp -r demo/assets site/assets; fi

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
